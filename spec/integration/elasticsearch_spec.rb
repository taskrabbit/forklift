require 'spec_helper'

describe 'elasticsearch' do  
  it "can read data (raw)"
  it "can read data (filtered)"
  it "can write new data"
  it "will write data with an assigned primary key when provided"
  it "can overwrite existing data"
  it "will refresh the index after writing to ensure that data is instantly searchable"
  it "can delete an index"
end